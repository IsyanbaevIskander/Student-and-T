kind: Service
apiVersion: v1
metadata:
  name: backend-app-service
  labels:
    app: backend-app
spec:
  type: ClusterIP
  selector:
    app: backend-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
