kind: Service
apiVersion: v1
metadata:
  name: backend-app-service
  labels:
    app: backend-app
spec:
  type: ClusterIP
  selector:
    app: backend-app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
